import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {domains: ["static.vecteezy.com", "www.paysit.net", "img.freepik.com", "res.cloudinary.com"]}
};

export default nextConfig;
